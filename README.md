# SesameTk

## How to start

1. generate project using `nb create` .
2. create your plugin using `nb plugin create` .
3. writing your plugins under `sesametk/plugins` folder.
4. run your bot using `nb run --reload` .

## Documentation

See [Docs](https://nonebot.dev/)
`nonebot-plugin-icey`
数据库迁移命令
nb orm revision -m "init_i18n"
nb orm upgrade

nb orm check